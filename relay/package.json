{
  "name": "pm-platform-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP relay that runs on Phil's Mac Mini. Receives chat requests from the deployed PM Platform on Vercel and answers them via the Claude Agent SDK (subscription OAuth), with document tools that query Supabase directly.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@supabase/supabase-js": "^2.106.1",
    "fastify": "^5.0.0",
    "zod": "^4.4.3"
  }
}
